# dlc-submit

This compatibility file is intentionally short to avoid duplicate skill discovery.

- Use `SKILL.md` for the active Codex skill workflow and safety rules.
- Use `references/pai-dlc-cli.md` for detailed Aliyun PAI DLC CLI examples and troubleshooting notes.
- Use `references/dsw-dlc-submit.md` for DSW-based submission defaults, logging, and mount preflight patterns.
